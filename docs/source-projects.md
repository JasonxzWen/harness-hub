# Source Projects

This file records upstream projects reviewed for Harness Hub. The install rule is personal distribution with a routing overlay: source ideas may come from any host ecosystem, imported skill bodies should keep upstream style by default, and committed distributed skills must stay in the standard `skills/<name>/SKILL.md` shape.

## Active Sources

| Source | Local status | Decision |
|---|---|---|
| `mattpocock/skills` | Refreshed at `999745ead1b37119380ad1f4de2bcdda5aa5bc84`; MIT | Installed `grill-me`, `diagnose`, `prototype`, `tdd-workflow`, and `handoff` after normalizing triggers and removing host-specific assumptions. The 2026-06-30 refresh keeps the default install surface unchanged while marking `codebase-design`, `domain-modeling`, and `writing-great-skills` as source ideas to fold into review/refactor rubrics, docs-consistency / LLM Wiki context rules, and skill-quality evals. |
| `multica-ai/andrej-karpathy-skills` | Refreshed at `2c606141936f1eeef17fa3043a72095b4765b9c2`; README and Claude plugin metadata declare MIT, but no standalone `LICENSE` file was present in the checked snapshot | Installed `karpathy-guidelines` as a standard helper skill for assumption surfacing, simplicity, surgical changes, and verifiable success criteria after an owner workflow selects implementation, review, or refactor scope. Did not import `CLAUDE.md`, `.cursor/rules/`, or `.claude-plugin/` host packaging. |
| `Fission-AI/OpenSpec` | Refreshed at `79303b521068c5f525ee61db06b915fc44b098f4`; replaces stale `open-spec/openspec` reference | Kept formal change lifecycle helpers; SDD remains the default change lane unless the user explicitly asks for OpenSpec or existing artifacts require it. |
| `obra/superpowers` | Refreshed at `f2cbfbefebbfef77321e4c9abc9e949826bea9d7`; reference-only source for broad-trigger workflow activation and brainstorming-style idea-shaping patterns | Used as routing and activation inspiration only; no wholesale workflow import. |
| `EveryInc/compound-engineering-plugin` | Refreshed at `f61d1b33eaba7be6e75902e31e49e022abdca3a0`; MIT | Installed only the `ce-code-review` workflow as `compound-code-review`; rejected wholesale import because it would duplicate many existing workflows. |
| `vercel-labs/skills` and `vercel-labs/agent-skills` | Refreshed at `c5ad3a85b4d16666974b161131413d08bfef3f7e` and `c39c4eb8a2981b0455e033882812c9df49bb72c5` | Selected web and React skills are kept when they remain standard skill folders with no host metadata. |
| `hluaguo/learn-faster-kit` | Refreshed at `209db9f6c873e3600a4b387748d82ffd24ddaf6e`; MIT | Adapted the learning lifecycle into `feynman-learning-coach`; did not import the larger runtime. |
| `zarazhangrui/frontend-slides` | Refreshed at `8dca834fc61abc9dd633cbe6a74ed7be3d82a608`; MIT | Reference source for `frontend-slides`; not merged into `effective-interact`. |
| `anthropics/skills` | Refreshed at `690f15cac7f7b4c055c5ab109c79ed9259934081`; plugin marketplace has `document-skills`, `example-skills`, and `claude-api` sets | Installed selective atoms: `mcp-builder`, `internal-comms`, `theme-factory`, `slack-gif-creator`; adapted slim atoms: `claude-api`, `skill-creator`; rewrote `doc-coauthoring` locally because the refreshed upstream directory had no per-skill license file. Do not import wholesale; document skills are source-available, not open source. |
| `walkinglabs/learn-harness-engineering` | Refreshed at `f31db7cd54ba222a1e62035ac0fd27dbbc6f8fd7`; public learning site and repository include harness-engineering lessons, example projects, resources, and a `harness-creator` skill/script | Evaluated as source material for Harness Hub. Local `validate-harness` and the internal `harness:minimal` template adapt its five-subsystem assessment, HTML report, structural benchmark framing, generic verification-command detection, durable validation records, checkpoint-commit policy, feature-state evidence, runtime signal capture, evaluator rubric, and quality snapshot ideas into the single `standard` target path. Do not import wholesale yet: root repository license evidence and host-specific metadata such as `agents/openai.yaml` must be resolved before redistribution. Use the local standard harness plus source records first. |
| `revfactory/harness` | Reviewed at `3fe5ad086e4c3e57afbe05a4f138225cad88e552`; Apache-2.0 | Source material only. Its team-architecture factory patterns, orchestrator templates, skill testing guidance, and QA boundary checks inform the single `standard` path through local `validate-harness` boundary checks, but do not add a separate harness level, optional pack, or Claude TeamCreate-dependent install surface. |
| `colbymchenry/codegraph` | Reviewed at `629d8472b14168841cd1f26b7022bf5934ff205d`; MIT; npm package `@colbymchenry/codegraph` observed at `0.9.9` | Added as an explicit external-tool configuration suggestion in `harness-hub check` and agent-readiness analysis. Do not import upstream `.claude/skills` or auto-write MCP config; keep installation, config review, and target indexing manual. |
| `chopratejas/headroom` | Reviewed at `982d01b9c996fd5fe26154dc2f94d567192f6ff6`; Apache-2.0 with NOTICE; Python package `headroom-ai` observed at `0.22.4` | Added as an explicit external-tool configuration suggestion for context compression, proxying, and shared-memory workflows. Do not enable by default; hooks, proxy, provider routing, telemetry, and memory behavior remain opt-in and reviewed. |
| `hardikpandya/stop-slop` | Refreshed at `8da1f030185bdfe8471220585162991eaeb970e9`; MIT | Installed as `stop-slop` with upstream `SKILL.md` and `references/` preserved except for local routing/source frontmatter. Trigger is narrow: English prose AI-tell cleanup, draft editing, or prose review only. Its bans on adverbs, passive voice, Wh- openers, and em dashes are strong style-editing rules, not default documentation policy. |
| `Leonxlnx/taste-skill` | Refreshed at `3c7017d636c3a4aad378433ea6d0cfa6c921da4a`; MIT | Installed a narrow `design-taste-frontend` standard skill for landing pages, portfolios, marketing pages, and redesigns. It is not a default frontend design skill and does not own dashboards, data tables, multi-step product UI, reports, or decks. Report-safe preflight ideas remain selectively absorbed into `effective-interact`. |
| `ThariqS/html-effectiveness` | Reviewed at `0e8d447494c81c661f2458b329e076a7ff7d75ec`; Apache-2.0; upstream README labels it sample code, not maintained, and not accepting contributions | Source pattern family and authorized visual-language reference for `effective-interact`, not an imported skill. Local adaptation generalizes its standalone HTML communication examples into disposable report, explanation, review, plan, status, incident, and local export-editor patterns with first-principles information architecture, source evidence, validation, ignored generated outputs, and installed activation coverage for upstream scenario families such as Exploration & Planning, Code Review & Understanding, Design, Reports, and Custom Editors. `skills/effective-interact/DESIGN.md` captures the reusable typography, spacing, color, border, and component-composition rules. Do not copy sample pages, fictional data, page-specific labels, or route production UI, slide decks, bundled apps, or throwaway prototype builds into `effective-interact`. |
| `JCodesMore/ai-website-cloner-template` | Refreshed at `c9b4fea5b257370af339abedad727c8903490dac`; MIT | Installed an explicit-only `clone-website` skill plus a lightweight `harness/website-cloner` smoke scaffold. Do not import the multi-platform host metadata or make website cloning a default frontend route; permission, browser evidence, replacement content, and validation are required. |
| Harness Hub local release-monitoring need | Added on 2026-06-05; local-original under MIT | Installed `package-release-sniffer` as a source-first helper for newly published AI/developer-tool package and model-package releases. It fills a recurring registry/release-feed discovery gap without adding registry clients, hooks, credentials, scheduled monitors, or package publishing side effects. |
| Harness Hub local quality/readiness audit need | Added on 2026-06-12; local-original under MIT | Installed `harness-quality-check` as an advisory HTML audit helper for Harness Hub source checkouts and target repositories. It composes existing self-check, agent-readiness, harness validation, and skill-quality evidence without adding enforcement, schedules, hooks, commits, pushes, or remote writes. |
| Harness Hub local interaction insight need | Added on 2026-06-25; local-original under MIT | Installed `insight` as a private, read-only repository interaction audit helper for Codex and Claude Code traces. It reads project-related local evidence broadly, separates confirmed and candidate evidence, writes ignored private reports by default, and does not mutate projects, memory, schedules, remotes, public artifacts, or tracked docs. |
| Harness Hub local LLM Wiki/context pack need | Added on 2026-06-29; local-original under MIT; informed by OpenAI Codex `AGENTS.md` docs, Claude Code memory docs, Obsidian Markdown vault docs, Cline/Roo memory-bank patterns, Basic Memory, and Supermemory | Added a standard harness context pack under `.harness-hub/context/`: schema, agent rules, stable Markdown wiki skeleton, contradiction register, update log, portable Obsidian profile, and validate-harness checks. The pack does not replace code search, raw sources, or agent-native memory; knowledge writes require human confirmation and preserve wiki content file-by-file during standard updates. |
## Evaluated But Not Installed

| Candidate | Decision | Reason |
|---|---|---|
| `NousResearch/hermes-agent` and `NousResearch/hermes-agent-self-evolution` | Reference-only | Reviewed on 2026-06-30; MIT. Useful for closeout insight design: on-demand skills, session/search evidence, agent-created skills, trace-backed eval datasets, guardrails, and PR-reviewed skill evolution. Do not install or import runtime code, `/learn`, cron, optimizer loops, DSPy/GEPA dependencies, or automatic skill mutation. Fold only bounded ideas into Harness Hub as source records, eval cases, human-reviewed skill candidates, or workflow guardrails. |
| `snarktank/ralph` | Retired from installable distribution | `ralph-prd`, `ralph-loop`, and `scripts/ralph/prd.json.example` were removed on 2026-05-22 because current Codex and Claude Code goal/story workflows cover the lane. Keep the upstream note as historical source evidence only. |
| Matt Pocock `writing-great-skills` | Reference-only | Useful skill-quality vocabulary: model-invoked vs user-invoked, description-as-router, progressive disclosure, completion criteria, no-op, sediment, and sprawl. Fold into `docs/skill-quality-guide.md` and routing/eval cases rather than installing another skill authoring lane; local Harness Hub quality gates remain authoritative. |
| Matt Pocock `grill-with-docs` and `domain-modeling` | Reference-only with candidate absorption | Useful for terminology pressure, scenario checks, code/doc contradiction surfacing, and sparing ADR triggers. Do not copy directly because inline `CONTEXT.md` and `docs/adr/` writes conflict with the standard harness LLM Wiki and state-file boundaries. Fold the ideas into `docs-consistency` loops and human-confirmed `.harness-hub/context` update rules. |
| `mattpocock/dictionary-of-ai-coding` | Reference-only | Reviewed at `2afee4819aaea83108bd662258ba5d35343dc7bb`; no license file or `package.json` license field was present in the checked snapshot. Use as terminology-system and explainer-structure inspiration for `docs/harness-vocabulary.md` and `effective-interact`; do not copy dictionary entry text, redistribute its contents, or install it as a skill. |
| `affaan-m/ECC` / `affaan-m/everything-claude-code` | Excluded from the atomic skill candidate pool | Removed by product direction on 2026-05-21 and reviewed at `64cd1ba248e77e377e76f70fc4e6434bfdddd511` under MIT. Do not use ECC as a source for new atom selection. Its stocktake, trigger-noise audit, and agent-architecture audit ideas inform local `validate-harness` checks when folded into the single `standard` path rather than shipped as another layer. |
| Matt Pocock `codebase-design` and `improve-codebase-architecture` | Wait, high-priority source ideas | Strong source material for deep modules, interfaces as test surfaces, deletion tests, dependency categories, replace-not-layer testing, and Design It Twice interface comparison. Do not install yet because they overlap review/refactor and SDD lanes; fold first into `compound-code-review`, `review-workflow`, `tdd-workflow`, or closeout technical-debt rubrics. |
| Matt Pocock `caveman` | Reject by default | Conflicts with this repo's communication policy unless explicitly requested. |
| Matt Pocock `to-prd`, `to-issues`, `triage`, `setup-matt-pocock-skills`, and `implement` | Reject by default | Overlap SDD/OpenSpec/local issue-routing surfaces or introduce external issue-tracker, label, `AGENTS.md`, `CLAUDE.md`, PRD, and issue side effects. Keep vertical-slice issue and durable agent-brief patterns as source ideas only. |
| Matt Pocock `resolving-merge-conflicts` | Reference-only | Useful reminder to inspect primary sources, preserve both intents, expose trade-offs, and rerun checks during PR closeout. Do not import the hard rule to always resolve and never abort; Harness Hub must stop for user-visible conflict or risk decisions when required. |
| Compound Engineering wholesale bundle | Reject by default | Too broad and overlap-heavy; custom agent files and external workflows should not enter the default skill tree. |
| `michalvavra/agents` `html-tools` | Explicit-only reference | Refreshed at `77473489d5c7b248af51ee7acf072c84a59ba6d4`; useful single-file HTML utility ideas, but overlaps `effective-interact` and `web-artifacts-builder`. |
| `Cocoon-AI/architecture-diagram-generator` | Explicit-only reference | Refreshed at `4b9087d55268c79a935105439dbcd37b630fc3f3`; useful architecture diagram pattern, but too narrow and CDN-oriented for default install. |
| Anthropic `docx`, `pdf`, `pptx`, `xlsx` | Reference-only | High-value document/spreadsheet/presentation/PDF source material, but source-available rather than open source in the refreshed snapshot, so not redistributed through Harness Hub. |
| Anthropic `brand-guidelines` | Reference-only | Apache 2.0, but it encodes Anthropic's own brand rather than a reusable customer brand workflow. |
| Anthropic `algorithmic-art` and `canvas-design` | Reference-only | Creative and useful as inspiration, but niche, heavier, and partially oriented around host artifact behavior. Keep out of distribution until a user need justifies a bounded standard skill. |
| Anthropic `frontend-design`, `web-artifacts-builder`, `webapp-testing` | Already covered | Existing local Harness Hub versions already cover these surfaces; do not add duplicates. |
| `supermemoryai/supermemory` | Reference-only | Reviewed at `253a82bdb757fdaa45c7705880f3c85ebdd8b686`; MIT. Useful as source material for agent memory architecture, explicit memory skills, project/user scoping, private-redaction rules, and MemoryBench-style evaluation. Do not import or enable by default yet: the practical integration path depends on hosted Supermemory credentials, hook side effects, and host-specific plugin surfaces such as `codex-supermemory`, `claude-supermemory`, and `opencode-supermemory`. Keep any future Harness Hub memory work explicit, opt-in, and validation-backed. |
| OpenAI Codex `AGENTS.md` docs and Anthropic Claude Code memory docs | Reference-only product docs | Codex uses `AGENTS.md` as project instruction context; Claude Code has `CLAUDE.md` imports and auto memory directories. Harness Hub should not duplicate host-native memory. The local context pack stores stable project wiki knowledge and keeps root target harness Codex-only by default. |
| Obsidian Markdown vault docs | Reference-only product docs | Obsidian's value for Harness Hub is convenience over local Markdown files. The standard harness ships only a portable `.obsidian` profile inside `.harness-hub/context/wiki/`, not full local app state, sync config, community plugins, or credentials. |
| Cline Memory Bank and Cline memory-bank docs | Reference-only | Useful for structured Markdown project memory, but the default file set includes volatile active context and progress concepts that overlap `.harness-hub/state/`. Harness Hub adopts the idea of durable Markdown context while rejecting wholesale rules that would duplicate task state. |
| Roo Code Memory Bank and Roo Code Memory Bank MCP variants | Reference-only | Useful evidence that file-based memory banks can survive session resets, but the repos are host-specific and often rely on Roo/Cline scanning or MCP runtime setup. Harness Hub keeps a platform-neutral Markdown wiki scaffold plus deterministic validation instead. |
| `basicmachines-co/basic-memory` and `basic-memory-skills` | Reference-only | Basic Memory provides local Markdown plus semantic-graph/MCP memory patterns, and `basic-memory-skills` shows small memory-management atoms. Do not install by default: Basic Memory itself is AGPL-3.0 and runtime/MCP/cloud setup adds licensing and operational surface beyond a credential-free standard harness. |

## Current Policy

- Read upstream README, skill files, plugin metadata, and license before deciding.
- Prefer preserving upstream skill bodies over local style rewrites.
- Use selective adaptation only when an upstream skill is unsafe, unusable, legally unclear, or directly conflicts with the routing overlay.
- Keep source attribution in skill metadata or this file.
- Keep host-specific tool names, config paths, runner scripts, and UI metadata out of local routing/workflow layers; do not rewrite imported bodies solely for this unless it blocks safe personal distribution.
- Keep third-party harness projects as source evidence until a concrete idea is ready to improve the single `standard` path. Do not create advanced harness packs, install levels, or bundle selectors.
- Update `docs/skill-routing.md`, `capabilities/index.json`, README, and tests when a candidate changes routing or the installable skill surface.
- See `docs/source-skill-inventory.md` for the latest local refresh, scanned skill names, and coverage notes.
